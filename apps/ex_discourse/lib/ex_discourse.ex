defmodule ExDiscourse do
  @moduledoc """
  ExDiscourse keeps the contexts that define your domain
  and business logic.

  Contexts are also responsible for managing your data, regardless
  if it comes from the database, an external API or others.
  """

  # @type ExDiscourse.result(t) :: {:ok, t} | {:error, t}
end
